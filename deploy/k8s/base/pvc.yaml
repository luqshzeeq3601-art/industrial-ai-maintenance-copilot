apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: copilot-vectorstore-pvc
  namespace: industrial-copilot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
