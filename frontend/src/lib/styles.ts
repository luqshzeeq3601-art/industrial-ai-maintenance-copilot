/** Shared form-control look: 40px (44px on touch), control border, accent focus ring. */
export const controlClass =
  "h-10 pointer-coarse:h-11 w-full rounded-[var(--radius-control)] border border-line-strong bg-panel text-small text-ink " +
  "hover:border-faint focus-visible:outline-2 focus-visible:outline-offset-0 focus-visible:outline-accent";
