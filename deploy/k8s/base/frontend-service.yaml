apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: industrial-copilot
  labels:
    app.kubernetes.io/name: copilot-frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      name: http-ui
  selector:
    app.kubernetes.io/name: copilot-frontend
