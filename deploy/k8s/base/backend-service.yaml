apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: industrial-copilot
  labels:
    app.kubernetes.io/name: copilot-backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http-api
  selector:
    app.kubernetes.io/name: copilot-backend
